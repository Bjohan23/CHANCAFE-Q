// importamos los modelos 
const User = require('../../auth/models/user');

// Exportamos los modelos como un objeto
module.exports = {
    User,
    // Aquí puedes añadir más modelos si es necesario
};