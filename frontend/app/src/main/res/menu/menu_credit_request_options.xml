<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Ver detalles -->
    <item
        android:id="@+id/action_view"
        android:title="Ver detalles"
        android:icon="@android:drawable/ic_menu_view" />

    <!-- Editar (solo para pendientes) -->
    <item
        android:id="@+id/action_edit"
        android:title="Editar"
        android:icon="@android:drawable/ic_menu_edit" />

    <!-- Aprobar (solo para pendientes) -->
    <item
        android:id="@+id/action_approve"
        android:title="Aprobar"
        android:icon="@android:drawable/ic_menu_agenda" />

    <!-- Rechazar (solo para pendientes) -->
    <item
        android:id="@+id/action_reject"
        android:title="Rechazar"
        android:icon="@android:drawable/ic_menu_close_clear_cancel" />

    <!-- Actualizar evaluación de riesgo (solo para pendientes) -->
    <item
        android:id="@+id/action_update_risk"
        android:title="Actualizar riesgo"
        android:icon="@android:drawable/ic_dialog_alert" />

    <!-- Eliminar (solo para pendientes y rechazadas) -->
    <item
        android:id="@+id/action_delete"
        android:title="Eliminar"
        android:icon="@android:drawable/ic_menu_delete" />

</menu>