<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ChancafeApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CHANCAFEQ"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        <!-- Actividad principal de Login -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dashboard principal -->
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Clientes -->
        <activity
            android:name=".ui.clients.ClientsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.dashboard.DashboardActivity" />

        <!-- Agregar/Editar Cliente -->
        <activity
            android:name=".ui.clients.AddEditClientActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.clients.ClientsActivity" />

        <!-- Selector de Cliente -->
        <activity
            android:name=".ui.clients.ClientSelectorActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.quotes.AddEditQuoteActivity" />

        <!-- Productos -->
        <activity
            android:name=".ui.products.ProductsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.dashboard.DashboardActivity" />

        <!-- Agregar/Editar Producto -->
        <activity
            android:name=".ui.products.AddEditProductActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.products.ProductsActivity" />

        <!-- Selector de Producto -->
        <activity
            android:name=".ui.products.ProductSelectorActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.quotes.AddEditQuoteItemActivity" />

        <!-- Cotizaciones -->
        <activity
            android:name=".ui.quotes.QuotesActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.dashboard.DashboardActivity" />

        <!-- Agregar/Editar Cotización -->
        <activity
            android:name=".ui.quotes.AddEditQuoteActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.quotes.QuotesActivity" />

        <!-- Detalle de Cotización -->
        <activity
            android:name=".ui.quotes.QuoteDetailActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.quotes.QuotesActivity" />

        <!-- Agregar/Editar Ítem de Cotización -->
        <activity
            android:name=".ui.quotes.AddEditQuoteItemActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.quotes.AddEditQuoteActivity" />

        <!-- Solicitudes de Crédito -->
        <activity
            android:name=".ui.credit.CreditRequestsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.dashboard.DashboardActivity" />

        <!-- Agregar/Editar Solicitud de Crédito -->
        <activity
            android:name=".ui.credit.AddEditCreditRequestActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.credit.CreditRequestsActivity" />

        <!-- Detalle de Solicitud de Crédito -->
        <activity
            android:name=".ui.credit.CreditRequestDetailActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.credit.CreditRequestsActivity" />

        <!-- Agenda -->
        <activity
            android:name=".ui.agenda.AgendaActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.dashboard.DashboardActivity" />

        <!-- Perfil -->
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:parentActivityName=".ui.dashboard.DashboardActivity" />

        <!-- MainActivity original (temporal) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>